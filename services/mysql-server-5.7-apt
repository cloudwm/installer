#!/bin/bash

if [ -f "include/startup.sh" ]; then
    . include/startup.sh
elif [ -f "../include/startup.sh" ]; then
    . ../include/startup.sh
fi

runOnceCheck ubuntu-updateos

# install mysql from apt
apt install mysql-server-5.7 -y | log
waitOrStop 0

# setting mysqld root password.


tag mysqld.success

