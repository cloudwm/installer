#
# This is the config file where we define what will be executed in the installer script.
#
# installer will read variables and will execute them one by one.
#
. installer-contrib-tweaks-almalinux-preinstall.conf

. installer-contrib-tweaks-almalinux-optimize.conf

execute+=(tweaks/cron-autoremove-old-logs)
execute+=(apps/cpanelwhm-liveinstall-almalinux-10-64-bit)
execute+=(tweaks/cwm-description-autoconfig)
